---
title: Gaervenel &amp; Elubar
---

h1 = current_page.data.title

ul
  - sitemap.resources.select { |resource| resource.data.title && !resource.data.adventure && resource != current_page }.each do |resource|
    li = link_to(resource.data.title, resource, relative: true)
